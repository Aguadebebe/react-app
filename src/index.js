import React from 'react';
import ReactDOM from 'react-dom/client';
import ChatInputOutput from './ ChatInput/ChatInput';
 

/*import App from "./ChatInputDkMd.js";*/
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
  <ChatInputOutput />
  
  </React.StrictMode>

);

 
